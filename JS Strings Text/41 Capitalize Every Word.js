/* 
Write a JavaScript function to capitalize each word in the string.

Test Data :

console.log(capitalizeWords('js string exercises'));
"JS STRING EXERCISES"
*/

function capitalizeWords(astr) {
  return astr.toUpperCase()
}

console.log(capitalizeWords('js string exercises'));