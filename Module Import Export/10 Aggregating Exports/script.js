/* 
Write a JavaScript program to aggregate exports from two different modules into one module and use them.
*/

import {add, subtract, divide, multiply} from './basic arithmetics.js'

console.log(add(9, 5));
console.log(subtract(9, 5));
console.log(multiply(9, 5));
console.log(divide(9, 9));